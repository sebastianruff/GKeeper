services:
  gkeeper:
    image: ${GKEEPER_IMAGE:-ghcr.io/your-github-user/gkeeper:latest}
    pull_policy: always
    ports:
      - "${GKEEPER_HOST_PORT:-5000}:5000"
    env_file:
      - .env
    restart: unless-stopped
